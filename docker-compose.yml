#
# `docker-compose` file used ONLY for hobby deployments.
#
# Please take a look at https://posthog.com/docs/self-host/deploy/hobby
# for more info.
#
# PostHog has sunset support for self-hosted K8s deployments.
# See: https://posthog.com/blog/sunsetting-helm-support-posthog
#

x-logging: &default-logging
    driver: json-file
    options:
        max-size: '50m'
        max-file: '3'

services:
    db:
        extends:
            file: docker-compose.base.yml
            service: db
        image: ${DOCKER_REGISTRY_PREFIX:-}postgres:15.12-alpine
        logging: *default-logging
        volumes:
            - postgres-data:/var/lib/postgresql/data

    redis7:
        extends:
            file: docker-compose.base.yml
            service: redis7
        logging: *default-logging
        ulimits:
            nproc: 65535
            nofile:
                soft: 65535
                hard: 65535
        volumes:
            - redis7-data:/data

    clickhouse:
        #
        # Note: please keep the default version in sync across
        #       `posthog` and the `charts-clickhouse` repos
        #
        extends:
            file: docker-compose.base.yml
            service: clickhouse
        restart: on-failure
        logging: *default-logging
        depends_on:
            - kafka
            - zookeeper
        volumes:
            - ./posthog/idl:/idl
            - ./docker/clickhouse/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d
            - ./docker/clickhouse/config.xml:/etc/clickhouse-server/config.xml
            - ./docker/clickhouse/config.d/default.xml:/etc/clickhouse-server/config.d/default.xml
            - ./docker/clickhouse/users.xml:/etc/clickhouse-server/users.xml
            - ./docker/clickhouse/user_defined_function.xml:/etc/clickhouse-server/user_defined_function.xml
            - ./posthog/user_scripts:/var/lib/clickhouse/user_scripts
            - clickhouse-data:/var/lib/clickhouse

    zookeeper:
        extends:
            file: docker-compose.base.yml
            service: zookeeper
        logging: *default-logging
        volumes:
            - zookeeper-datalog:/datalog
            - zookeeper-data:/data
            - zookeeper-logs:/logs
    kafka:
        extends:
            file: docker-compose.base.yml
            service: kafka
        logging: *default-logging
        depends_on:
            - zookeeper
        environment:
            KAFKA_LOG_RETENTION_MS: 3600000
            KAFKA_LOG_RETENTION_CHECK_INTERVAL_MS: 300000
            KAFKA_LOG_RETENTION_HOURS: 1
        volumes:
            - kafka-data:/bitnami/kafka

    worker:
        extends:
            file: docker-compose.base.yml
            service: worker
        logging: *default-logging
        volumes:
            - ./ee/hogai:/code/ee/hogai
            - ./ee/settings.py:/code/ee/settings.py
            - ./posthog/temporal:/code/posthog/temporal
            - ./products/ai_observability:/code/products/ai_observability
            - ./products/signals:/code/products/signals
            - ./products/tasks:/code/products/tasks
            - ./services/llm-gateway/src/llm_gateway:/code/services/llm-gateway
            - ./tools/pr-approval-agent:/code/tools/pr-approval-agent
        environment:
            CYCLOTRON_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            CYCLOTRON_NODE_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            PERSONS_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            BEHAVIORAL_COHORTS_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            FLAGS_READ_STORE_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            SITE_URL: https://$DOMAIN
            SECRET_KEY: $POSTHOG_SECRET
            OBJECT_STORAGE_ENDPOINT: '${OBJECT_STORAGE_ENDPOINT:-https://s3.eu-west-2.amazonaws.com}'
            OBJECT_STORAGE_ACCESS_KEY_ID: ${OBJECT_STORAGE_ACCESS_KEY_ID}
            OBJECT_STORAGE_SECRET_ACCESS_KEY: ${OBJECT_STORAGE_SECRET_ACCESS_KEY}
            OBJECT_STORAGE_PUBLIC_ENDPOINT: https://$DOMAIN
            OBJECT_STORAGE_REGION: ${OBJECT_STORAGE_REGION:-eu-west-2}
            AWS_REGION: ${AWS_REGION:-eu-west-2}
            AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-eu-west-2}
            SESSION_RECORDING_V2_S3_BUCKET: ${SESSION_RECORDING_V2_S3_BUCKET:-posthog-disinfo}
            SESSION_RECORDING_V2_S3_REGION: ${SESSION_RECORDING_V2_S3_REGION:-eu-west-2}
            SESSION_RECORDING_V2_S3_ENDPOINT: '${SESSION_RECORDING_V2_S3_ENDPOINT:-https://s3.eu-west-2.amazonaws.com}'
            SESSION_RECORDING_V2_S3_ACCESS_KEY_ID: ${SESSION_RECORDING_V2_S3_ACCESS_KEY_ID}
            SESSION_RECORDING_V2_S3_SECRET_ACCESS_KEY: ${SESSION_RECORDING_V2_S3_SECRET_ACCESS_KEY}
            OBJECT_STORAGE_ENABLED: true
            RECORDING_API_URL: http://recording-api:6738
            ENCRYPTION_SALT_KEYS: $ENCRYPTION_SALT_KEYS
            POSTHOG_SKIP_MIGRATION_CHECKS: '1'
            PERSONHOG_ADDR: 'personhog-router:50052'
            PERSONHOG_ENABLED: 'true'
            PERSONHOG_ROLLOUT_PERCENTAGE: '100'
            REDIS_URL: redis://redis7:6379
            LOGS_REDIS_HOST: redis7
            LOGS_REDIS_PORT: 6379
            JOIN_REDIS_HOST: redis7
            # AI provider keys for PostHog's AI features. Blank unless set in .env.
            ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
            OPENAI_API_KEY: ${OPENAI_API_KEY:-}
        image: $REGISTRY_URL:$POSTHOG_APP_TAG
        depends_on:
            db:
                condition: service_healthy
            redis7:
                condition: service_healthy
            clickhouse:
                condition: service_started
            kafka:
                condition: service_healthy
            web:
                condition: service_started
            personhog-router:
                condition: service_started
    web:
        extends:
            file: docker-compose.base.yml
            service: web
        command: /compose/start
        logging: *default-logging
        volumes:
            - ./compose:/compose
            - ./ee/hogai:/code/ee/hogai
            - ./ee/settings.py:/code/ee/settings.py
            - ./posthog/temporal:/code/posthog/temporal
            - ./products/ai_observability:/code/products/ai_observability
            - ./products/signals:/code/products/signals
            - ./products/tasks:/code/products/tasks
            - ./services/llm-gateway/src/llm_gateway:/code/services/llm-gateway
            - ./tools/pr-approval-agent:/code/tools/pr-approval-agent
        image: $REGISTRY_URL:$POSTHOG_APP_TAG
        environment:
            CYCLOTRON_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            CYCLOTRON_NODE_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            PERSONS_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            BEHAVIORAL_COHORTS_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            FLAGS_READ_STORE_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            SITE_URL: https://$DOMAIN
            LIVESTREAM_HOST: 'https://${DOMAIN}/livestream'
            SECRET_KEY: $POSTHOG_SECRET
            OBJECT_STORAGE_REGION: ${OBJECT_STORAGE_REGION:-eu-west-2}
            AWS_REGION: ${AWS_REGION:-eu-west-2}
            AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-eu-west-2}
            OBJECT_STORAGE_ACCESS_KEY_ID: ${OBJECT_STORAGE_ACCESS_KEY_ID}
            OBJECT_STORAGE_SECRET_ACCESS_KEY: ${OBJECT_STORAGE_SECRET_ACCESS_KEY}
            SESSION_RECORDING_V2_S3_BUCKET: ${SESSION_RECORDING_V2_S3_BUCKET:-posthog-disinfo}
            SESSION_RECORDING_V2_S3_REGION: ${SESSION_RECORDING_V2_S3_REGION:-eu-west-2}
            SESSION_RECORDING_V2_S3_ACCESS_KEY_ID: ${SESSION_RECORDING_V2_S3_ACCESS_KEY_ID}
            SESSION_RECORDING_V2_S3_SECRET_ACCESS_KEY: ${SESSION_RECORDING_V2_S3_SECRET_ACCESS_KEY}
            OBJECT_STORAGE_ENDPOINT: '${OBJECT_STORAGE_ENDPOINT:-https://s3.eu-west-2.amazonaws.com}'
            SESSION_RECORDING_V2_S3_ENDPOINT: '${SESSION_RECORDING_V2_S3_ENDPOINT:-https://s3.eu-west-2.amazonaws.com}'
            OBJECT_STORAGE_PUBLIC_ENDPOINT: '${OBJECT_STORAGE_PUBLIC_ENDPOINT:-https://posthog-disinfo.s3.eu-west-2.amazonaws.com}'
            OBJECT_STORAGE_ENABLED: true
            RECORDING_API_URL: http://recording-api:6738
            ENCRYPTION_SALT_KEYS: $ENCRYPTION_SALT_KEYS
            OTEL_SERVICE_NAME: 'posthog'
            OTEL_EXPORTER_OTLP_ENDPOINT: ''
            OTEL_SDK_DISABLED: 'true'
            OPT_OUT_CAPTURE: ${OPT_OUT_CAPTURE:-false}
            DEPLOYMENT: 'hobby'
            PERSONHOG_ADDR: 'personhog-router:50052'
            PERSONHOG_ENABLED: 'true'
            PERSONHOG_ROLLOUT_PERCENTAGE: '100'
            REDIS_URL: redis://redis7:6379
            LOGS_REDIS_HOST: redis7
            LOGS_REDIS_PORT: 6379
            JOIN_REDIS_HOST: redis7
            TEMPORAL_HOST: temporal
            # AI provider keys for PostHog's AI features. Blank unless set in .env.
            ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
            OPENAI_API_KEY: ${OPENAI_API_KEY:-}
        depends_on:
            - db
            - redis7
            - clickhouse
            - kafka
            - objectstorage
            - seaweedfs
            - personhog-router

    plugins:
        extends:
            file: docker-compose.base.yml
            service: plugins
        image: ${REGISTRY_URL}-node:${POSTHOG_NODE_TAG:-latest}
        logging: *default-logging
        environment:
            SITE_URL: https://$DOMAIN
            SECRET_KEY: $POSTHOG_SECRET
            OBJECT_STORAGE_REGION: ${OBJECT_STORAGE_REGION:-eu-west-2}
            OPENAI_API_KEY: $OPENAI_API_KEY
            AWS_REGION: ${AWS_REGION:-eu-west-2}
            AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-eu-west-2}
            OBJECT_STORAGE_ENDPOINT: '${OBJECT_STORAGE_ENDPOINT:-https://s3.eu-west-2.amazonaws.com}'
            OBJECT_STORAGE_ACCESS_KEY_ID: ${OBJECT_STORAGE_ACCESS_KEY_ID}
            OBJECT_STORAGE_SECRET_ACCESS_KEY: ${OBJECT_STORAGE_SECRET_ACCESS_KEY}
            SESSION_RECORDING_V2_S3_BUCKET: ${SESSION_RECORDING_V2_S3_BUCKET:-posthog-disinfo}
            SESSION_RECORDING_V2_S3_REGION: ${SESSION_RECORDING_V2_S3_REGION:-eu-west-2}
            SESSION_RECORDING_V2_S3_ACCESS_KEY_ID: ${SESSION_RECORDING_V2_S3_ACCESS_KEY_ID}
            SESSION_RECORDING_V2_S3_SECRET_ACCESS_KEY: ${SESSION_RECORDING_V2_S3_SECRET_ACCESS_KEY}
            SESSION_RECORDING_V2_S3_ENDPOINT: '${SESSION_RECORDING_V2_S3_ENDPOINT:-https://s3.eu-west-2.amazonaws.com}'
            OBJECT_STORAGE_PUBLIC_ENDPOINT: '${OBJECT_STORAGE_PUBLIC_ENDPOINT:-https://posthog-disinfo.s3.eu-west-2.amazonaws.com}'
            OBJECT_STORAGE_ENABLED: true
            SESSION_RECORDING_V2_S3_TIMEOUT_MS: 120000
            CDP_REDIS_HOST: redis7
            CDP_REDIS_PORT: 6379
            LOGS_REDIS_HOST: redis7
            LOGS_REDIS_PORT: 6379
            REDIS_URL: redis://redis7:6379
            ENCRYPTION_SALT_KEYS: $ENCRYPTION_SALT_KEYS
            CYCLOTRON_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            PERSONS_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            BEHAVIORAL_COHORTS_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            OTEL_EXPORTER_OTLP_ENDPOINT: ''
        volumes:
            - ./share:/share
            - ./services/llm-gateway/src/llm_gateway:/code/services/llm-gateway
            - ./products/signals:/code/products/signals
            - ./products/tasks:/code/products/tasks
        depends_on:
            - db
            - redis7
            - clickhouse
            - kafka
            - objectstorage
            - seaweedfs

    ingestion-general:
        extends:
            file: docker-compose.base.yml
            service: ingestion-general
        image: ${REGISTRY_URL}-node:${POSTHOG_NODE_TAG:-latest}
        logging: *default-logging
        environment:
            PERSONS_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            BEHAVIORAL_COHORTS_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            ENCRYPTION_SALT_KEYS: $ENCRYPTION_SALT_KEYS
        depends_on:
            - db
            - redis7
            - clickhouse
            - kafka

    ingestion-sessionreplay:
        extends:
            file: docker-compose.base.yml
            service: ingestion-sessionreplay
        image: ${REGISTRY_URL}-node:${POSTHOG_NODE_TAG:-latest}
        logging: *default-logging
        environment:
            SESSION_RECORDING_V2_S3_BUCKET: ${SESSION_RECORDING_V2_S3_BUCKET:-posthog-disinfo}
            SESSION_RECORDING_V2_S3_REGION: ${SESSION_RECORDING_V2_S3_REGION:-eu-west-2}
            SESSION_RECORDING_V2_S3_ACCESS_KEY_ID: ${SESSION_RECORDING_V2_S3_ACCESS_KEY_ID}
            SESSION_RECORDING_V2_S3_SECRET_ACCESS_KEY: ${SESSION_RECORDING_V2_S3_SECRET_ACCESS_KEY}
            SESSION_RECORDING_V2_S3_ENDPOINT: '${SESSION_RECORDING_V2_S3_ENDPOINT:-https://s3.eu-west-2.amazonaws.com}'
            SESSION_RECORDING_V2_S3_TIMEOUT_MS: 120000
            ENCRYPTION_SALT_KEYS: $ENCRYPTION_SALT_KEYS
        volumes:
            - ./products/signals:/code/products/signals
            - ./products/tasks:/code/products/tasks
        depends_on:
            - db
            - redis7
            - kafka
            - seaweedfs

    recording-api:
        extends:
            file: docker-compose.base.yml
            service: recording-api
        image: ${REGISTRY_URL}-node:${POSTHOG_NODE_TAG:-latest}
        logging: *default-logging
        environment:
            ENCRYPTION_SALT_KEYS: $ENCRYPTION_SALT_KEYS
        depends_on:
            - db
            - redis7
            - clickhouse

    ingestion-error-tracking:
        extends:
            file: docker-compose.base.yml
            service: ingestion-error-tracking
        image: ${REGISTRY_URL}-node:${POSTHOG_NODE_TAG:-latest}
        logging: *default-logging
        environment:
            ENCRYPTION_SALT_KEYS: $ENCRYPTION_SALT_KEYS
        depends_on:
            - db
            - redis7
            - kafka

    ingestion-logs:
        extends:
            file: docker-compose.base.yml
            service: ingestion-logs
        image: ${REGISTRY_URL}-node:${POSTHOG_NODE_TAG:-latest}
        logging: *default-logging
        environment:
            LOGS_REDIS_HOST: ""
            LOGS_REDIS_PORT: 6379
            REDIS_URL: redis://redis7:6379
        depends_on:
            - db
            - redis7
            - kafka

    ingestion-traces:
        extends:
            file: docker-compose.base.yml
            service: ingestion-traces
        image: ${REGISTRY_URL}-node:${POSTHOG_NODE_TAG:-latest}
        logging: *default-logging
        environment:
            LOGS_REDIS_HOST: redis7
            LOGS_REDIS_PORT: 6379
            REDIS_URL: redis://redis7:6379
        depends_on:
            - db
            - redis7
            - kafka

    proxy:
        extends:
            file: docker-compose.base.yml
            service: proxy
        logging: *default-logging
        ports:
            - '80:80'
            - '4443:443'
        volumes:
            - caddy-data:/data
            - caddy-config:/config
        environment:
            CADDY_TLS_BLOCK: $TLS_BLOCK
            CADDY_HOST: '$DOMAIN, http://, https://'
        depends_on:
            - web
            - livestream
    objectstorage:
        extends:
            file: docker-compose.base.yml
            service: objectstorage

        restart: on-failure
        logging: *default-logging
        volumes:
            - objectstorage:/data
        ports:
            - '19000:19000'
            - '19001:19001'

    seaweedfs:
        extends:
            file: docker-compose.base.yml
            service: seaweedfs
        container_name: '${SEAWEEDFS_DOCKER_NAME:-seaweedfs-main}'
        restart: on-failure
        logging: *default-logging
        ports:
            - '127.0.0.1:8333:8333' # S3 API
            - '127.0.0.1:9333:9333' # Master server
        volumes:
            - seaweedfs:/data
        healthcheck:
            test:
              [
                  'CMD',
                  'sh',
                  '-c',
                  "echo 's3.bucket.list' | /usr/bin/weed shell -master=localhost:9333 2>&1 | grep -q posthog",
              ]
            interval: 5s
            timeout: 10s
            retries: 30
            start_period: 10s

    sftp:
        image: atmoz/sftp:latest
        container_name: posthog_sftp${CONTAINER_PREFIX:-}
        logging: *default-logging
        ports:
            - '${SFTP_PORT:-2222}:22'
        volumes:
            - objectstorage:/home/${SFTP_USER:-sftp_user}/objectstorage:rw
            - seaweedfs:/home/${SFTP_USER:-sftp_user}/seaweedfs:rw
            - postgres-data:/home/${SFTP_USER:-sftp_user}/postgres-data:rw
            - clickhouse-data:/home/${SFTP_USER:-sftp_user}/clickhouse-data:rw
            - kafka-data:/home/${SFTP_USER:-sftp_user}/kafka-data:rw
        environment:
            - SFTP_USERS=${SFTP_USER:-sftp_user}:${SFTP_PASSWORD:-password}:1001
        restart: unless-stopped
        entrypoint: |
            sh -c "
            for dir in objectstorage seaweedfs postgres-data clickhouse-data kafka-data; do
              chown -R 1001:1001 /home/${SFTP_USER:-sftp_user}/$$dir &&
              chmod -R 777 /home/${SFTP_USER:-sftp_user}/$$dir;
            done &&
            /entrypoint ${SFTP_USER:-sftp_user}:${SFTP_PASSWORD:-password}:1001
            "
        command: ${SFTP_USER:-sftp_user}:${SFTP_PASSWORD:-password}:1001

    asyncmigrationscheck:
        extends:
            file: docker-compose.base.yml
            service: asyncmigrationscheck
        image: $REGISTRY_URL:$POSTHOG_APP_TAG
        logging: *default-logging
        environment:
            SITE_URL: https://$DOMAIN
            SECRET_KEY: $POSTHOG_SECRET
            SKIP_ASYNC_MIGRATIONS_SETUP: 0

    # Temporal containers
    temporal:
        extends:
            file: docker-compose.base.yml
            service: temporal
        logging: *default-logging
        environment:
            - ENABLE_ES=false
        ports:
            - 7233:7233
        volumes:
            - ./docker/temporal/dynamicconfig:/etc/temporal/config/dynamicconfig
    elasticsearch:
        extends:
            file: docker-compose.base.yml
            service: elasticsearch
        logging: *default-logging
    temporal-admin-tools:
        extends:
            file: docker-compose.base.yml
            service: temporal-admin-tools
        logging: *default-logging
        depends_on:
            - temporal
    temporal-ui:
        extends:
            file: docker-compose.base.yml
            service: temporal-ui
        logging: *default-logging
        ports:
            - 8081:8080
        depends_on:
            temporal:
                condition: service_started
            db:
                condition: service_healthy
    temporal-django-worker:
        command: /compose/temporal-django-worker
        extends:
            file: docker-compose.base.yml
            service: temporal-django-worker
        logging: *default-logging
        volumes:
            - ./compose:/compose
            - ./ee/hogai:/code/ee/hogai
            - ./ee/settings.py:/code/ee/settings.py
            - ./posthog/temporal:/code/posthog/temporal
            - ./products/ai_observability:/code/products/ai_observability
            - ./products/signals:/code/products/signals
            - ./products/tasks:/code/products/tasks
            - ./services/llm-gateway/src/llm_gateway:/code/services/llm-gateway
            - ./tools/pr-approval-agent:/code/tools/pr-approval-agent
        image: $REGISTRY_URL:$POSTHOG_APP_TAG
        environment:
            SITE_URL: https://$DOMAIN
            SECRET_KEY: $POSTHOG_SECRET
        depends_on:
            - db
            - redis7
            - clickhouse
            - kafka
            - objectstorage
            - seaweedfs
            - temporal

    # Max AI worker - requires OPENAI_API_KEY to be set
    # ANTHROPIC_API_KEY is optional (only for /ticket command)
    # See: https://posthog.com/docs/self-host/deploy/hobby#max-ai-setup
    temporal-django-worker-max-ai:
        command: /compose/temporal-django-worker
        extends:
            file: docker-compose.base.yml
            service: temporal-django-worker
        volumes:
            - ./compose:/compose
            - ./ee/hogai:/code/ee/hogai
            - ./ee/settings.py:/code/ee/settings.py
            - ./posthog/temporal:/code/posthog/temporal
            - ./products/ai_observability:/code/products/ai_observability
            - ./products/signals:/code/products/signals
            - ./products/tasks:/code/products/tasks
            - ./services/llm-gateway/src/llm_gateway:/code/services/llm-gateway
            - ./tools/pr-approval-agent:/code/tools/pr-approval-agent
        image: $REGISTRY_URL:$POSTHOG_APP_TAG
        environment:
            SITE_URL: https://$DOMAIN
            SECRET_KEY: $POSTHOG_SECRET
            TEMPORAL_TASK_QUEUE: max-ai-task-queue
            OPENAI_API_KEY: ${OPENAI_API_KEY}
            ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
            HOGAI_ANTHROPIC_MODEL: ${HOGAI_ANTHROPIC_MODEL}
        depends_on:
            - db
            - redis7
            - clickhouse
            - kafka
            - objectstorage
            - seaweedfs
            - temporal

    cyclotron-janitor:
        extends:
            file: docker-compose.base.yml
            service: cyclotron-janitor
        build:
            context: ./rust
        logging: *default-logging
        environment:
            DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            KAFKA_HOSTS: 'kafka:9092'
        depends_on:
            db:
                condition: service_healthy
            kafka:
                condition: service_started

    capture:
        build:
            context: ./rust
        extends:
            file: docker-compose.base.yml
            service: capture
        logging: *default-logging

    replay-capture:
        build:
            context: ./rust
        extends:
            file: docker-compose.base.yml
            service: replay-capture
        logging: *default-logging

    property-defs-rs:
        build:
            context: ./rust
        extends:
            file: docker-compose.base.yml
            service: property-defs-rs
        logging: *default-logging
        depends_on:
            kafka-init:
                condition: service_completed_successfully
            db:
                condition: service_healthy

    livestream:
        extends:
            file: docker-compose.base.yml
            service: livestream
        logging: *default-logging
        environment:
            - LIVESTREAM_JWT_SECRET=${POSTHOG_SECRET}
        volumes:
            - ./docker/livestream/configs-hobby.yml:/configs/configs.yml

    personhog-replica:
        build:
            context: ./rust
        extends:
            file: docker-compose.base.yml
            service: personhog-replica
        logging: *default-logging
        environment:
            PRIMARY_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
        depends_on:
            db:
                condition: service_healthy

    personhog-router:
        build:
            context: ./rust
        extends:
            file: docker-compose.base.yml
            service: personhog-router
        logging: *default-logging
        depends_on:
            - personhog-replica

    feature-flags:
        build:
            context: ./rust
        extends:
            file: docker-compose.base.yml
            service: feature-flags
        logging: *default-logging
        environment:
            PERSONS_WRITE_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            PERSONS_READ_DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
        depends_on:
            - db
            - redis7

    hypercache-server:
        build:
            context: ./rust
        extends:
            file: docker-compose.base.yml
            service: hypercache-server
        logging: *default-logging
        depends_on:
            - redis7

    kafka-init:
        image: docker.redpanda.com/redpandadata/redpanda:v25.1.9
        entrypoint: /bin/sh
        logging: *default-logging
        command:
            - -c
            - |
                set -x
                echo "Waiting for Kafka broker to accept connections..."
                TIMEOUT=60
                ELAPSED=0
                # Use rpk topic list as the readiness check - it's a simple operation
                # that confirms the broker is accepting Kafka protocol requests
                until rpk topic list --brokers kafka:9092 2>/dev/null; do
                    echo "Kafka broker not ready yet (elapsed: $${ELAPSED}s)..."
                    sleep 2
                    ELAPSED=$$((ELAPSED + 2))
                    if [ $$ELAPSED -ge $$TIMEOUT ]; then
                        echo "Timeout waiting for Kafka broker after $${TIMEOUT}s"
                        echo "Final attempt to list topics:"
                        rpk topic list --brokers kafka:9092 || true
                        exit 1
                    fi
                done
                echo "Kafka broker is accepting requests, creating topics..."
                # Create topics needed by rust services and the combined analytics ingestion
                # consumer, which checks topic existence at startup for every output it produces
                # to (see analytics/outputs/registry.ts). Auto-create only fires on producer-first
                # writes, so pre-create them here.
                # Note: $$ escapes $ for docker-compose variable substitution
                TOPICS="clickhouse_events_json \
                    clickhouse_ai_events_json \
                    clickhouse_heatmap_events \
                    clickhouse_ingestion_warnings \
                    events_plugin_ingestion_dlq \
                    events_plugin_ingestion_overflow \
                    events_plugin_ingestion_async \
                    clickhouse_groups \
                    clickhouse_person \
                    clickhouse_person_distinct_id \
                    clickhouse_app_metrics2 \
                    log_entries \
                    clickhouse_tophog"
                for topic in $$TOPICS; do
                    if rpk topic create "$$topic" --brokers kafka:9092 -p 1 -r 1 2>&1; then
                        echo "Topic $$topic created successfully"
                    else
                        if rpk topic list --brokers kafka:9092 | grep -q "$$topic"; then
                            echo "Topic $$topic already exists, continuing"
                        else
                            echo "Failed to create topic $$topic"
                            exit 1
                        fi
                    fi
                done
                echo "Final topic list:"
                rpk topic list --brokers kafka:9092
                echo "Topics ready"
        depends_on:
            kafka:
                condition: service_healthy

    cymbal:
        image: ghcr.io/posthog/posthog/cymbal:master
        build:
            context: ./rust
            args:
                BIN: cymbal
        restart: on-failure
        logging: *default-logging
        volumes:
            - ./share:/share
            - ./products/signals:/code/products/signals
            - ./products/tasks:/code/products/tasks
        environment:
            KAFKA_HOSTS: 'kafka:9092'
            OBJECT_STORAGE_REGION: ${OBJECT_STORAGE_REGION:-eu-west-2}
            AWS_REGION: ${AWS_REGION:-eu-west-2}
            AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-eu-west-2}
            OBJECT_STORAGE_BUCKET: ${OBJECT_STORAGE_BUCKET:-posthog-disinfo}
            OBJECT_STORAGE_ENDPOINT: '${OBJECT_STORAGE_ENDPOINT:-https://s3.eu-west-2.amazonaws.com}'
            OBJECT_STORAGE_ACCESS_KEY_ID: ${OBJECT_STORAGE_ACCESS_KEY_ID}
            OBJECT_STORAGE_SECRET_ACCESS_KEY: ${OBJECT_STORAGE_SECRET_ACCESS_KEY}
            OBJECT_STORAGE_FORCE_PATH_STYLE: 'true'
            BIND_HOST: '0.0.0.0'
            BIND_PORT: '3302'
            DATABASE_URL: 'postgres://posthog:posthog@db:5432/posthog'
            PERSONS_URL: 'postgres://posthog:posthog@db:5432/posthog'
            MAXMIND_DB_PATH: '/share/GeoLite2-City.mmdb'
            REDIS_URL: 'redis://redis7:6379/'
            ISSUE_BUCKETS_REDIS_URL: 'redis://redis7:6379/'
            RUST_LOG: 'info'
        depends_on:
            kafka-init:
                condition: service_completed_successfully
            seaweedfs:
                condition: service_healthy
            db:
                condition: service_healthy
            redis7:
                condition: service_healthy

volumes:
    zookeeper-data:
    zookeeper-datalog:
    zookeeper-logs:
    objectstorage:
    seaweedfs:
    postgres-data:
    clickhouse-data:
    caddy-data:
    caddy-config:
    redis7-data:
    kafka-data:
    redpanda-data:
